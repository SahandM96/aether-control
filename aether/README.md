# Aether helpers (binary not committed)

Download the official Windows build from upstream:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\download-aether.ps1
```

Or from the repo root:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\setup.ps1
```

Upstream project: https://github.com/CluvexStudio/Aether  
Release asset: `aether-windows-x86_64.zip` (tested: v1.2.0)
